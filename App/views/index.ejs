<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo App - 3-Tier Architecture</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <!-- Header -->
        <header class="app-header">
            <h1>📝 Todo Application</h1>
            <p class="subtitle">3-Tier AWS Architecture Demo</p>
            <div class="server-info">
                <span class="badge">🖥️ <%= hostname %></span>
                <span class="badge">🌍 <%= environment %></span>
            </div>
        </header>

        <!-- Add Todo Form -->
        <div class="card">
            <h2>➕ Add New Todo</h2>
            <form id="addTodoForm">
                <div class="form-group">
                    <input 
                        type="text" 
                        id="todoTitle" 
                        placeholder="Enter todo title..." 
                        required 
                        autocomplete="off"
                    >
                </div>
                <div class="form-group">
                    <textarea 
                        id="todoDescription" 
                        placeholder="Enter description (optional)..." 
                        rows="3"
                    ></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Add Todo</button>
            </form>
        </div>

        <!-- Stats Section -->
        <div class="stats-container">
            <div class="stat-card">
                <div class="stat-value" id="totalTodos">0</div>
                <div class="stat-label">Total Tasks</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="completedTodos">0</div>
                <div class="stat-label">Completed</div>
            </div>
            <div class="stat-card">
                <div class="stat-value" id="pendingTodos">0</div>
                <div class="stat-label">Pending</div>
            </div>
        </div>

        <!-- Filter Buttons -->
        <div class="filter-buttons">
            <button class="btn btn-filter active" data-filter="all">All</button>
            <button class="btn btn-filter" data-filter="active">Active</button>
            <button class="btn btn-filter" data-filter="completed">Completed</button>
            <button class="btn btn-danger btn-clear" id="clearCompleted">Clear Completed</button>
        </div>

        <!-- Todo List -->
        <div class="card">
            <h2>📋 Your Todos</h2>
            <div id="todoList" class="todo-list">
                <div class="loading">Loading todos...</div>
            </div>
            <div id="emptyState" class="empty-state" style="display: none;">
                <p>🎉 No todos yet! Add your first task above.</p>
            </div>
        </div>

        <!-- Footer -->
        <footer class="app-footer">
            <p>Built with ❤️ by Joel Livingstone Kofi Ackah</p>
            <p class="tech-stack">Node.js • Express • MySQL/PostgreSQL • Docker • AWS</p>
        </footer>
    </div>

    <!-- Toast Notification -->
    <div id="toast" class="toast"></div>

    <script src="/js/app.js"></script>
</body>
</html>
